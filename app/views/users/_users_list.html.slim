table#setDataTable.display.table.table-bordered.table-hover style="margin-bottom:0; margin-top: 10px; width: 100%;" data-source="#{users_path(format: :json)}"
  thead
    tr
      th Id
      th Username
      th Email
      th Role
      th Created at
      th 

  tbody

javascript:
  jQuery(document).ready(function() {
    $('#setDataTable').dataTable({
      "processing": true,
      "serverSide": true,
      "ajax": $('#setDataTable').data('source'),
      "pagingType": "full_numbers",
      "responsive": true,
      "order": [[ 0, "desc" ]],
      "dom": '<"top"f>rt<"bottom"ip><"clear">',
      "columns": [
        {"data": "id"},
        {"data": "username"},
        {"data": "email"},
        {"data": "role"},
        {"data": "created_at"},
        {"data": "action"}
      ],
      "oLanguage": { "sSearch": "Search: " },
      "columnDefs": [ 
        {
          "targets": 5,
          "orderable": false
        } 
      ]
    });
  });